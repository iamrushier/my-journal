# MongoDB (local, dockerized)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=admin
MONGO_DB_NAME=journaldb
MONGO_URI=mongodb://admin:admin@mongo:27017/journaldb?authSource=admin

# Redis (local, dockerized)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=admin

# Email (dev only – no password required; empty by default)
EMAIL_ADDRESS=
EMAIL_APP_PASSWORD=

# Weather API (dev only)
WEATHER_API_KEY=

# Spring Profile
SPRING_PROFILES_ACTIVE=dev
